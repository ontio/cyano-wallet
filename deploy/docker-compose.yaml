version: '3.2'
services:
    wallet:
        image: wallet:dev
        build: .
        volumes:
            - /src

    nginx:
        image: onyxchain_wallet:dev
        build: 
            context: .
            dockerfile: Dockerfile.srv
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - type: volume
              source: .
              target: /host
              volume:
                   noncopy: true
        links:
            - wallet 
